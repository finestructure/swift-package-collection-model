import XCTest
@testable import swift_package_collection_model

final class swift_package_collection_modelTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_package_collection_model().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
